spring:
  cloud:
    config:
      name: book-service
      discovery:
        enabled: true
        service-id: config
      fail-fast: true
      retry:
        max-attempts: 10
      allow-override: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/