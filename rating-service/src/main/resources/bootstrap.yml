spring:
  cloud:
    config:
      name: rating-service
      discovery:
        enabled: true
        service-id: config
      fail-fast: true
      retry:
        max-attempts: 10
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/